FROM python:3.12-slim

RUN apt-get update && apt-get install -y --no-install-recommends \
    curl wget git jq nodejs npm && \
    rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir requests

RUN useradd -m -s /bin/bash user
USER user
WORKDIR /workspace

CMD ["sleep", "infinity"]
