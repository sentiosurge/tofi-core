name: Write File
description: 将内容写入产物文件

nodes:
  write:
    type: shell
    config:
      # 默认写入产物目录
      script: 'echo "$CONTENT" > "$TOFI_ARTIFACTS_DIR/{{inputs.filename}}"'
      env:
        CONTENT: "inputs.content"